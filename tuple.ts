let person:[string, number, boolean] = ["John Doe", 30, true];
console.log(person[0]);

let employee:[string, number, string, boolean] = ["Jane Smith", 25, "Software Engineer", true];
console.log(employee[1]);

let product:[string, number, boolean] = ["Laptop", 999.99, true];
console.log(product[2]);